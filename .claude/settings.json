{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in */data/*) python3 \"$CLAUDE_PROJECT_DIR/scripts/validate-data.py\" >&2 || exit 2 ;; esac",
            "statusMessage": "Validating data/*.json"
          }
        ]
      }
    ]
  }
}
